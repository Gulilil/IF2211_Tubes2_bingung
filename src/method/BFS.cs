using System;
using System.Diagnostics;

namespace src
{
    public class BFS
    {
        private int nodes;
        private int steps;
        private char[] routes;
        private Point[] paths;
        private Stopwatch watch;

        // ctor
        public BFS()
        {
            this.nodes = 0;
            this.steps = 0;
            this.routes = null;
            this.paths = null;
        }

        // setter getter

        // other methods

    }
}